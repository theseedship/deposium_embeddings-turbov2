# Requirements for Qwen2.5-1.5B-Instruct Model2Vec Distillation
# Use with: pip install -r requirements_qwen25.txt

# Core dependencies
model2vec>=0.6.0              # 0.6.0+ fixes tokenizer bug, 0.7.0+ has simplified API
torch==2.8.0                  # PyTorch (CPU or CUDA)
transformers>=4.50.0          # For Qwen2.5 model loading
tokenizers>=0.19.0            # Tokenizer support

# Model2Vec internals
sentence-transformers>=3.0.0  # Required by model2vec
scikit-learn>=1.3.0           # For PCA and metrics
numpy>=1.26.0,<2.0.0          # Array operations
safetensors>=0.4.0            # Model serialization

# Evaluation dependencies
scipy>=1.11.0                 # Statistical functions
tqdm>=4.66.0                  # Progress bars

# Optional: GPU acceleration (comment out if CPU-only)
# accelerate>=1.0.0           # For faster GPU loading

# Optional: MTEB for full benchmarks (large download)
# mteb>=1.18.0                # Full MTEB evaluation suite

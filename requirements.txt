fastapi==0.115.6
uvicorn==0.32.1
model2vec==0.3.0
pydantic==2.9.2
numpy==1.26.4
safetensors==0.4.5

# EmbeddingGemma dependencies (PyTorch CPU-only for Railway)
torch==2.6.0
transformers==4.46.3
sentence-transformers==3.3.1

# Qwen3 Reranker with 4-bit quantization (GPU)
bitsandbytes==0.45.0
accelerate==1.2.1

# Optimum + ONNX Runtime for optimized CPU inference
optimum[onnxruntime]==1.23.3
onnxruntime==1.20.1

# MTEB for model evaluation (optional, dev only)
# mteb==1.18.0
